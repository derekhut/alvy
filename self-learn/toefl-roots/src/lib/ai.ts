// V2 stub — AI-powered features (mnemonics, explore mode)
// Will use OpenAI client when implemented

export function generateMnemonic(_word: string, _root: string): Promise<string> {
  return Promise.resolve("Coming in V2");
}
